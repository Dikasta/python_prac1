concept1 : in programming i observed list taking much iterration space as compaire to set .for less memory use, use set not list.

exp: ans =set()
ans.add((nums[i], nums[j], nums[l], nums[r]))
-------------------------------
'''
concept 2: def removeDuplicates(self, nums: List[int]) -> int:
        i=0
        #print(nums)
        while i<len(nums)-1:
            if nums[i]==nums[i+1]: # this will work on sorted list
                nums.pop(i)
            else:
                i+=1
        #return
note:- when i will perform in-place array/list on that case for given method not require return types
'''
